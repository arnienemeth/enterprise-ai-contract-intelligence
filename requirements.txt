# Enterprise AI Contract Intelligence Platform
# Runtime dependencies (verified working — app boots and endpoints respond)
#
# Install:  pip install -r requirements.txt
# Python:   3.10+ recommended

# --- Web / API layer ---
fastapi==0.139.2
uvicorn[standard]==0.51.0
python-multipart==0.0.32        # required by /upload-contract (file uploads)

# --- Azure OpenAI (chat + embeddings) ---
openai==2.46.0                  # AzureOpenAI client

# --- Vector store / semantic search ---
chromadb==1.5.9                 # local dev vector DB (./vector_db)
azure-search-documents==11.5.2  # production vector store (used when AZURE_SEARCH_ENDPOINT is set)

# --- Document ingestion / OCR (real PDF/DOCX/scans) ---
pdfplumber==0.11.9              # digital PDF text extraction (primary)
pypdf==6.13.1                   # PDF fallback extractor
python-docx==1.2.0              # DOCX text + tables
azure-ai-documentintelligence==1.0.2  # OCR for scans/images (used when AZURE_DOCINTEL_ENDPOINT is set)

# --- Config ---
python-dotenv==1.2.2            # loads .env

# --- MCP server (mcp_server/server.py — reserved for future integration) ---
mcp==1.28.1

# Note: on some sandboxed/proxied environments httpx needs the socks extra:
#   pip install "httpx[socks]"
# Not required on a normal Windows/Mac/Linux workstation.
